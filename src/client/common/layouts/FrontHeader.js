import React, { Component } from 'react';

export default class FrontHeader extends Component {
    render() {
        return (
            <header>
                Header
            </header>
        );
    }
}
